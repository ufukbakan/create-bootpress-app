{
  "name": "$project_name$",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "prestart": "npm run build",
    "start": "node dist/index.js",
    "dev": "node --watch index.js",
    "test": "vitest --run",
    "test:coverage": "vitest --run --coverage",
    "build": "esbuild index.js --bundle --format=esm --target=es2022 --platform=node --outdir=dist --minify --packages=external"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootpress": "11.0.0-rc.10",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@vitest/coverage-v8": "4.0.18",
    "esbuild": "^0.27.3",
    "pino-pretty": "^13.1.3",
    "supertest": "^7.2.2",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
