{
  "name": "create-bootpress-app",
  "version": "5.0.0-rc.4",
  "description": "CLI tool to create a bootpress app",
  "main": "bundle.js",
  "type": "commonjs",
  "bin": "bundle.js",
  "engines": {
    "node": ">=10"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ufukbakan/create-bootpress-app"
  },
  "keywords": [],
  "author": "Ufuk Bakan",
  "license": "MIT",
  "files": [
    "./bundle.js",
    "./template"
  ],
  "dependencies": {
    "colors": "^1.4.0",
    "commander": "^10.0.0",
    "keypress": "^0.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "eslint": "^8.35.0"
  },
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --target=node10 --outfile=bundle.js --minify --tree-shaking=true",
    "prepublishOnly": "npm run build"
  }
}